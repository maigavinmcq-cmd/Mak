# `account_video_data` 使用说明 SOP

## 1. 工具用途

`account_video_data.exe` 用于抓取当前已打开的 TikTok 账号主页中的视频链接、发布日期、播放量，并导出为 Excel。

适用场景：

- 批量统计多个 TikTok 账号在指定日期范围内的视频数量
- 导出视频明细用于复盘、对账、数据整理
- 按分组管理账号并输出分组汇总

---

## 2. 使用前准备

请先确认目标电脑满足以下条件：

1. 已安装 `Microsoft Edge` 或 `Google Chrome`
2. 能正常访问并登录 TikTok
3. 已拿到程序目录：
   - `account_video_data/account_video_data.exe`

说明：

- 工具会自动启动一个“专用浏览器”
- 不建议直接用你平时工作的浏览器窗口来抓取

---

## 3. 首次使用流程

### 第一步：启动程序

双击打开：

- `account_video_data.exe`

### 第二步：启动专用浏览器

点击界面中的：

- `启动专用浏览器`

程序会自动打开一个专用的 Edge/Chrome 浏览器窗口。

### 第三步：登录 TikTok

在这个新打开的浏览器里：

1. 登录你的 TikTok 账号
2. 保持浏览器窗口不要关闭

### 第四步：打开待抓取账号主页

你可以手动打开账号主页，也可以直接把账号填到工具中。

如果账号主页没有打开，程序在正式抓取前会自动补开。

---

## 4. 界面说明

### 快速开始

- `启动专用浏览器`
  - 自动启动工具专用浏览器
- `开始抓取`
  - 开始执行抓取任务
- `停止任务`
  - 发送停止信号，当前步骤结束后会停止

### 基础设置

- `开始日期`
  - 抓取起始日期
- `结束日期`
  - 抓取结束日期
- `时区`
  - 统计发布日期时使用的时区
- `导出文件`
  - Excel 输出路径
- `选择路径`
  - 选择导出文件位置
- `打开文件`
  - 抓取成功后快速打开最新导出的 Excel

### 账号分组输入

- `组名`
  - 当前账号组的名称
- 下方文本框
  - 每行一个账号
  - 支持三种格式：
    - `username`
    - `@username`
    - `https://www.tiktok.com/@username`

### 分组操作

- `保存/更新`
  - 保存当前组
- `加载选中组`
  - 把列表中选中的组加载回编辑区
- `删除选中组`
  - 删除组列表中选中的组
- `清空编辑区`
  - 清空当前输入内容

### 高级设置 / 调试工具

普通使用一般不需要动这里。

- `调试浏览器地址`
  - 默认 `http://127.0.0.1:9222`
- `检测浏览器连接`
  - 检查当前浏览器是否可连接
- `列出已打开标签页`
  - 查看当前浏览器中已打开的标签页
- `清空日志`
  - 清空下方日志显示

---

## 5. 日常抓取标准流程

### 方式 A：按分组抓取

1. 启动程序
2. 点击 `启动专用浏览器`
3. 在浏览器里登录 TikTok
4. 在工具里填写：
   - 开始日期
   - 结束日期
   - 时区
   - 导出路径
5. 在“账号分组输入”里：
   - 输入组名
   - 每行粘贴一个账号
6. 点击 `保存/更新`
7. 在下方列表中选中要抓取的组
8. 点击 `开始抓取`

### 方式 B：只抓当前编辑区账号

1. 启动程序
2. 启动专用浏览器并登录 TikTok
3. 在编辑区输入账号
4. 不一定要先保存成组
5. 点击 `开始抓取`

---

## 6. 自动处理逻辑说明

程序开始抓取前，会自动做这些事：

1. 自动检查浏览器是否已启动
2. 如果没启动，会自动尝试启动专用浏览器
3. 如果待抓取账号主页已经打开：
   - 会逐个刷新
   - 一个刷新完成后再处理下一个
4. 如果待抓取账号主页没有打开：
   - 会自动逐个补开
   - 一个页面基本加载完成后再打开下一个

这样做的目的：

- 避免一次性打开太多页面
- 避免昨天残留页面缓存导致抓到旧数据

---

## 7. 导出结果说明

程序会导出一个带时间戳的 Excel 文件，例如：

```text
tiktok_open_tabs_results_20260402_153001.xlsx
```

这样即使你上一次导出的文件还打开着，也不会因为同名占用而失败。

Excel 中通常包含：

- 视频明细
- 账号汇总
- 分组汇总

视频明细中包含字段：

- `group_name`
- `username`
- `publish_date`
- `video_id`
- `play_count`
- `video_url`
- `source_tab`

---

## 8. 账号输入规范

推荐每行一个账号，例如：

```text
account_a
@account_b
https://www.tiktok.com/@account_c
```

注意：

- 不要一行放多个账号
- 不要夹杂无关文字
- 程序会自动去重

---

## 9. 常见使用建议

### 建议 1：第一次使用先少量测试

建议先用 1 到 3 个账号试跑，确认：

- 浏览器能正常打开
- TikTok 能正常登录
- Excel 能正常导出

### 建议 2：抓取期间不要关闭专用浏览器

抓取过程中请不要关闭程序自动启动的浏览器，否则可能导致任务中断。

### 建议 3：账号很多时不要手工一次开太多标签页

因为程序已经支持自动补开和自动刷新，通常不需要你自己手工把所有主页都打开。

---

## 10. 常见问题

### Q1：如果昨天打开过账号主页，今天还要不要手动刷新？

不用。

当前版本在正式抓取前会自动逐个刷新已打开的目标账号主页。

### Q2：如果账号主页还没打开，程序能不能自己打开？

可以。

程序会自动补开未打开的账号主页，并且按顺序一个个打开，避免一次性开太多。

### Q3：为什么导出文件名每次都不一样？

因为程序会自动在文件名后加时间戳，避免上一个 Excel 还在打开时导致保存失败。

### Q4：如果抓取失败怎么办？

先检查：

1. 专用浏览器是否已打开
2. 是否已经登录 TikTok
3. 待抓取页面是否是账号主页而不是视频详情页
4. 下方运行日志中是否有明确报错

---

## 11. 推荐给小白的最简操作法

如果你完全不懂技术，直接照下面做：

1. 打开 `account_video_data.exe`
2. 点击 `启动专用浏览器`
3. 在浏览器里登录 TikTok
4. 在工具里填日期
5. 把账号一行一个粘贴进去
6. 点击 `开始抓取`
7. 抓取完成后点击 `打开文件`

---

## 12. 文件分发建议

给其他电脑使用时，建议直接发整个目录：

- `dist/account_video_data`

不要只单独发 `exe`。

原因是：

- `onedir` 版本依赖目录内其他运行文件
- 单独复制 `exe` 可能无法正常运行

